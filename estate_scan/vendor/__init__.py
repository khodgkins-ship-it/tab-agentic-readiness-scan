"""Vendored third-party code. See NOTICE and LICENSE.

Only `query_validator` (with its `schema_loader` shim and `types-and-filters.md`
reference) is vendored here, from tableau/tableau-metadata-explorer under the
Apache License 2.0. It runs over our fixed query set as a build-time check; it is
never part of the runtime scan path.
"""
